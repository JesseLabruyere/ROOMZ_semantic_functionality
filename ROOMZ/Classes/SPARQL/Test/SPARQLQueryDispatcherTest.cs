using System;
using System.Collections.Generic;
using VDS.RDF.Query;

namespace ROOMZ
{
	public class SPARQLQueryDispatcherTest
	{
		/*public static void Main() 
		{
			Console.WriteLine ("Starting");

			// The portfolios endpoint
			if (false) 
			{
				SPARQLQueryDispatcher queryDispatcher = new SPARQLQueryDispatcher (new Uri ("http://195.93.238.56:3030/portfolios/query"));
				SparqlResultSet result = queryDispatcher.dispatchQuery ("SELECT ?subject ?predicate ?object WHERE { ?subject ?predicate ?object } LIMIT 25");
				Console.WriteLine (result.Count);
				List<SparqlResult> triples = result.Results;
				foreach(SparqlResult triple in triples) 
				{
					Console.WriteLine (triple);
				}
			}

			// The dbpedia endpoint
			if (false) 
			{
				SPARQLQueryDispatcher queryDispatcher2 = new SPARQLQueryDispatcher (new Uri ("http://dbpedia.org/sparql"));
				SparqlResultSet result2 = queryDispatcher2.dispatchQuery ("SELECT ?subject ?predicate ?object WHERE { ?subject ?predicate ?object } LIMIT 25");
				Console.WriteLine (result2.Count);
				List<SparqlResult> triples2 = result2.Results;
				foreach(SparqlResult triple in triples2) 
				{
					Console.WriteLine (triple);
				}
			}
		}*/
	}
}
	